# 发布基线 · 2026-09-21

本仓库以「江畔蟹事」2026-09-21 已部署版本为起点。首次提交为独立快照，不携带此前私有 Git 历史、环境文件、数据库、订单、收货信息、备份或依赖目录。

## 包含的修复

- 「中国·江都」落款进入正常文档流，随页面内容出现在底部，与导航响应式对齐。
- 移除残留的双框文字印章，改为已有 SVG 图标。
- 普通购买、套装、建团、参团、编辑与团长成单统一校验季节停售和缺货，旧选择允许减少或移除。
- 拼团可售配置随轮询刷新，失败时提示重试；历史订单与幂等重放保留原快照。

## 发布整理

应用源码 `src/`、`server/src/`、数据库迁移与站点素材保持部署版本内容。测试中的内部管理员样例替换为虚构值；演示脚本要求通过 `ADMIN_CODE` 显式传入管理员下单码。运维文档脱敏，部署说明改为通用自托管步骤。

README 封面重新编排项目现有水彩素材；流程图根据现有功能制作，数据均为虚构，不是原始页面截图。图片在 `docs/showcase/`，不修改运行中的网站界面。

## 验证

- 前端生产构建通过。
- 后端 189 项测试通过。
- 前端可售规则回归 5 项通过。
- 地址解析回归 36 项通过。

本基线没有在线支付、自动物流跟踪或通用品类管理。鸡蛋与禽鱼等农产品是可改造方向，当前运行规则仍面向大闸蟹。

## 授权

当前未添加开源许可证；公开可见不等于另行授予代码或图像的使用、改作与再分发许可。需要相关授权时请联系仓库所有者。
